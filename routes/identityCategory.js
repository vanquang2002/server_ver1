// import express from "express";
// import { IdentityCategoryController } from "../controllers/index.js";

// const identityCategoryRouter = express.Router();

// // GET: /identity-categories -> Get all identity categories
// identityCategoryRouter.get("/", IdentityCategoryController.getIdentityCategories);

// // GET: /identity-categories/:id -> Get identity category by Id
// identityCategoryRouter.get("/:id", IdentityCategoryController.getIdentityCategoryById);

// // // POST: /identity-categories -> Create a new identity category
// // identityCategoryRouter.post("/", IdentityCategoryController.createIdentityCategory);

// // // PUT: /identity-categories/:id -> Update identity category by Id
// // identityCategoryRouter.put("/:id", IdentityCategoryController.editIdentityCategory);

// // // DELETE: /identity-categories/:id -> Delete identity category by Id
// // identityCategoryRouter.delete("/:id", IdentityCategoryController.deleteIdentityCategory);

// export default identityCategoryRouter;
