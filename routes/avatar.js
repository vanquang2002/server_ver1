// import express from "express";
// import { AvatarController } from "../controllers/index.js";

// const avatarRouter = express.Router();

// // GET: /avatars -> Get all avatars
// avatarRouter.get("/", AvatarController.getAvatars);

// // GET: /avatars/:id -> Get avatar by Id
// avatarRouter.get("/:id", AvatarController.getAvatarById);

// // POST: /avatars -> Create a new avatar
// avatarRouter.post("/", AvatarController.createAvatar);

// // PUT: /avatars/:id -> Update avatar by Id
// avatarRouter.put("/:id", AvatarController.editAvatar);

// // DELETE: /avatars/:id -> Delete avatar by Id
// avatarRouter.delete("/:id", AvatarController.deleteAvatar);

// export default avatarRouter;
